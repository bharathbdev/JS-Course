const message: string = "Hello from TypeScript!";
console.log(message);


//npx tsc test.ts && node test.js



























export {}; // makes this file a module — fixes "Cannot redeclare" error
